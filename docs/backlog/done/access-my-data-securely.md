Access my data in a secure manner.

Squad endpoint protected by a native API Gateway key
(`docs/adrs/002-native-api-gateway-key-for-squad-endpoint.md`), consumed
by a native Android app (`biwenger-client-android`) that holds and sends
it as `x-api-key`. Verified working end to end on a physical device.
