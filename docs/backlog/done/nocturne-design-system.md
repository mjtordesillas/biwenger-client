Make the app visually appealing, following a design system (Nocturne —
dark, Inter, restrained purple accent) and a provided HTML mockup for the
squad screen.

Shipped in `biwenger-client-android`: tokens ported to a Compose theme
(`ui/theme/`), squad screen rebuilt to match the mockup — header, tappable
position filter chips, player rows with real photo/crest images and a
colored price trend, a player detail bottom sheet. Bottom nav bar and
mockup stats without real data behind them (Rank, Budget, Form) were
deliberately left out. Verified on a physical device through several
rounds of feedback.
